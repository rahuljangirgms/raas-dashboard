export type User = {
    id: string
    meta_id: string
    email: string
    display_name: string
    first_name: string
    last_name: string
    phone_number: string
    role: string
    status: string
    is_active: boolean
    created_at: string
    [key: string]: any
}
